# Дата: 2026-03-30

Под конец недели решила пододелывать те таски, которые оставались на Dashboard нашем.
Это включало рефакторинг работы с localStorage так как мы ввели позже сервис для этого, но не везде перешли на него.
Поправила импорты delay, где нужно было.
Перепроверила и оптимизировала код Theme Switcher, своего компонента.

И вторым шагом была реализация Language Swircher нового компонента.
Конечно внешний вид самого переключателя максимально похож на существующий Theme Switcher.
Стейты сохраняются в localStorage.
И я решила дореализовывать его кастомно, так как почитав, выяснилось что нужно добавить 1-2 функции.
Как
public t(): typeof EN {
return DICTIONARIES[this.getLang()];
}

public getLocalizedField(field: ILocalizedString): string {
return field[this.getLang()];
}

Потом прошлась по коду и применила функции перевода там где требуется.

- **Мысли / Планы:**
  Можно порефакторить код ещё и сделать что-то для Dashboard.

- **Затраченное время:** около 4-6 часов
