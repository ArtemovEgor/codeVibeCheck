# Дата: 2025-03-08

- **Что было сделано:**
  Реализовала Practice страницу с Quiz виджетом от загрузки данных до показа результата (без статистики пользователя)
  - **Проблемы:**
    Основная сложность это поиск архитектурного решения, а именно: как передавать verdict обратно в стратегию,
    где хранить состояние виджета, как сделать стратегии универсальными для будущих типов.
    Всё это нужно чтобы после валидации сразу показывать результат прямо в виджете.

- **Решения (или попытки):**
  Расширила IWidgetStrategy методом showVerdict(verdict, widget) для показа результата

- **Мысли / Планы:**
  Quiz работает, но это только один из четырёх типов виджетов.
  Следующие шаги TrueFalse виджет, Library страница и в какой-то момент прогресс пользователя для статистики и т.д.

- **Затраченное время:**
  Около 10 часов за 3 дня
