# 05.04.2026

Закончена работа над бэкендом для топиков/виджетов, включавшая в себя 3 этапа:

1. В основной файл БД добавлено создание 5 таблиц:

- topics
- topic_requirements
- widgets
- user_topic_progress
- user_learning_stats

2. На основе мок-данных фронта были сгенерированы 2 JSON файла с топиками/виджетами. Сделана проверка, заполнены ли таблицы `topics`, `topic_requirements`, `widgets`. Если в таблицах нет данных, происходит заполнение на основе JSON файлов.

###

3. Сделаны эндпойнты для взаимодействия с фронтом:

- GET /api/topics
- GET /api/topics/:id
- GET /api/topics/:id/widgets
- GET /api/widgets/:id
- POST /api/widgets/:id/submit
- GET /api/progress
- GET /api/progress/stats
- GET /api/progress/:topicId
- POST /api/progress/:topicId/init
- POST /api/progress
- PATCH /api/progress/:topicId/reset

По-хорошему, то, что получилось, требует рефакторинга, т.к. файлы разрослись до 600 - 700 строк и явно требуют фрагментации.
